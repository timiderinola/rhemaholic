$("<%= escape_javascript(render @micropost) %>").prependTo("#posts").addClass("new_post")
$("#microposts_count").html('<%= pluralize(current_user.microposts.count, "Post") %>')
$('#new_micropost').bind('ajax:success', function(){
    $("form").trigger("reset");
})